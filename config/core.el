;; Here is the root of your personal configs.
;; Either place config straight in here, or load external files such as this bindings file:

;; Load personal bindings
(load-dotfile "config/bindings.el")
(load-dotfile "config/cosmetic.el")
